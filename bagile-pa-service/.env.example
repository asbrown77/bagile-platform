# BAgile PA Service — environment variables
# Copy to .env for local development (not needed for MCP stdio — env injected via .mcp.json)

# User running this PA session (alex | chris)
PA_USER_ID=alex
